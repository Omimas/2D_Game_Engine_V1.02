#include "Engine.h"

int main(int argc, char* argv[]) {
    Engine engine;

    if (!engine.initialize()) {
        std::cerr << "Engine initialization failed!" << std::endl;
        return -1;
    }

    engine.run();

    return 0;
}